
  CREATE OR REPLACE TRIGGER "CORP_MOBILE"."TIU_OPTIONS_ENABLED_FOR_TARIFS" 
  BEFORE INSERT OR UPDATE ON OPTIONS_ENABLED_FOR_TARIFFS FOR EACH ROW 
--#Version=1
BEGIN 
  IF INSERTING THEN 
    IF NVL(:NEW.ID, 0) = 0 then 
      :NEW.ID := S_NEW_OPTIONS_ENABL_FOR_TAR_ID.NEXTVAL; 
    END IF; 
    :NEW.USER_CREATED := USER; 
    :NEW.DATE_CREATED := SYSDATE;
  END IF; 
  :NEW.USER_LAST_UPDATED := USER; 
  :NEW.DATE_LAST_UPDATED := SYSDATE; 
END;
--#end if
ALTER TRIGGER "CORP_MOBILE"."TIU_OPTIONS_ENABLED_FOR_TARIFS" ENABLE