
  CREATE OR REPLACE TRIGGER "CORP_MOBILE"."TIU_REPORT_TYPES" 
  BEFORE INSERT OR UPDATE ON REPORT_TYPES FOR EACH ROW
--#Version=1
BEGIN
  IF INSERTING THEN
    IF NVL(:NEW.REPORT_TYPE_ID, 0) = 0 then
      :NEW.REPORT_TYPE_ID := NEW_REPORT_TYPE_ID;
    END IF;
  END IF;
END; 
ALTER TRIGGER "CORP_MOBILE"."TIU_REPORT_TYPES" ENABLE