
  CREATE OR REPLACE TRIGGER "CORP_MOBILE"."TIU_BILL_FOR_DILER_SAVED" 
  BEFORE INSERT OR UPDATE ON BILL_FOR_DILER_SAVED FOR EACH ROW
  --Version=1
BEGIN
  IF INSERTING THEN
    :NEW.USER_CREATED := USER;
    :NEW.DATE_CREATED := SYSDATE;
  END IF;  
  :NEW.USER_LAST_UPDATED := USER;
  :NEW.DATE_LAST_UPDATED := SYSDATE;
END; 
ALTER TRIGGER "CORP_MOBILE"."TIU_BILL_FOR_DILER_SAVED" ENABLE