
  CREATE OR REPLACE TRIGGER "CORP_MOBILE"."TI_REPLACE_SIM_LOG" 
  BEFORE INSERT ON  REPLACE_SIM_LOG FOR EACH ROW
--#Version=1
BEGIN
     :NEW.rep_date := sysdate;
     :NEW.rep_user := sys_context ('USERENV','CURRENT_USER');
END;

ALTER TRIGGER "CORP_MOBILE"."TI_REPLACE_SIM_LOG" ENABLE