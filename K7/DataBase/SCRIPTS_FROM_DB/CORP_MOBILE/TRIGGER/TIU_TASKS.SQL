
  CREATE OR REPLACE TRIGGER "CORP_MOBILE"."TIU_TASKS" 
  BEFORE INSERT OR UPDATE ON TASKS FOR EACH ROW
--#Version=1
BEGIN
  if INSERTING then
    :NEW.Insert_Date:=sysdate;
  end if;
    :NEW.UPDATE_DATE := sysdate;
END;

ALTER TRIGGER "CORP_MOBILE"."TIU_TASKS" ENABLE