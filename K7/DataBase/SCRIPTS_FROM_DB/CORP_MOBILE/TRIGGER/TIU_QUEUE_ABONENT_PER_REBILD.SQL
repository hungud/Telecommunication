
  CREATE OR REPLACE TRIGGER "CORP_MOBILE"."TIU_QUEUE_ABONENT_PER_REBILD" 
  BEFORE INSERT OR UPDATE ON QUEUE_ABONENT_PER_REBILD FOR EACH ROW 
--#Version=1
BEGIN 
  IF INSERTING THEN 
    IF :NEW.DATE_INSERT IS NULL then 
      :NEW.DATE_INSERT := SYSDATE; 
    END IF; 
  END IF;
END; 
ALTER TRIGGER "CORP_MOBILE"."TIU_QUEUE_ABONENT_PER_REBILD" ENABLE