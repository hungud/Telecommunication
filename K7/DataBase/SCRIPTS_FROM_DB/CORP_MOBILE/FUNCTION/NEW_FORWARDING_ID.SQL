
  CREATE OR REPLACE FUNCTION "CORP_MOBILE"."NEW_FORWARDING_ID" RETURN INTEGER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_FORWARDING_ID.NEXTVAL
    INTO vRES
    FROM DUAL;
  RETURN vRES;
END; 