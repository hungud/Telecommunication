
  CREATE OR REPLACE FUNCTION "CORP_MOBILE"."NEW_PHONE_BALANCE_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_PHONE_BALANCE_ID.NEXTVAL
  INTO vRES
  FROM DUAL;
  RETURN vRES;
END; 