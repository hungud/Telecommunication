
  CREATE OR REPLACE FUNCTION "CORP_MOBILE"."NEW_OPTION_GROUP_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_OPTION_GROUP_ID.NEXTVAL
  INTO vRES
  FROM DUAL;
  RETURN vRES;
END; 