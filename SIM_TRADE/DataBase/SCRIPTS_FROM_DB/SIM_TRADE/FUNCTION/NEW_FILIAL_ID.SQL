
  CREATE OR REPLACE FUNCTION "SIM_TRADE"."NEW_FILIAL_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  --raise_application_error(-20000, '!');
  SELECT S_NEW_FILIAL_ID.NEXTVAL
  INTO vRES
  FROM DUAL;
  RETURN vRES;
END; 