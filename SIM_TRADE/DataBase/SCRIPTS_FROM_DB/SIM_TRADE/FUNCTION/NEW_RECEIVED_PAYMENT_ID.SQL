
  CREATE OR REPLACE FUNCTION "SIM_TRADE"."NEW_RECEIVED_PAYMENT_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_RECEIVED_PAYMENT_ID.NEXTVAL
  INTO vRES
  FROM DUAL;
  RETURN vRES;
END; 