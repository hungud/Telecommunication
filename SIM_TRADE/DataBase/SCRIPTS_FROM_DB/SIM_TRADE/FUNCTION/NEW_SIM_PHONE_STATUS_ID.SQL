
  CREATE OR REPLACE FUNCTION "SIM_TRADE"."NEW_SIM_PHONE_STATUS_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_SIM_PHONE_STATUS_ID.NEXTVAL
    INTO vRES
    FROM DUAL;
  RETURN vRES;
END; 