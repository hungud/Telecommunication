
  CREATE OR REPLACE FUNCTION "SIM_TRADE"."NEW_CURRENT_PHONE_OPTION_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_CURRENT_PHONE_OPTION_ID.NEXTVAL
    INTO vRES
    FROM DUAL;
  RETURN vRES;
END; 