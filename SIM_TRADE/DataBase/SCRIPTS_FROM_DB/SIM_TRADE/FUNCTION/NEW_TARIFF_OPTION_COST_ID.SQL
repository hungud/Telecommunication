
  CREATE OR REPLACE FUNCTION "SIM_TRADE"."NEW_TARIFF_OPTION_COST_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_TARIFF_OPTION_COST_ID.NEXTVAL
  INTO vRES
  FROM DUAL;
  RETURN vRES;
END; 