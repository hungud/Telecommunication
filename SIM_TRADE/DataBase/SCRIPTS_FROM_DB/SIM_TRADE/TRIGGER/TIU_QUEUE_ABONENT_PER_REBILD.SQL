
  CREATE OR REPLACE TRIGGER "SIM_TRADE"."TIU_QUEUE_ABONENT_PER_REBILD" 
--#Version=1
  BEFORE INSERT OR UPDATE ON QUEUE_ABONENT_PER_REBILD FOR EACH ROW
BEGIN 
  IF INSERTING THEN 
    IF :NEW.DATE_INSERT IS NULL then 
      :NEW.DATE_INSERT := SYSDATE; 
    END IF; 
  END IF;
END;
ALTER TRIGGER "SIM_TRADE"."TIU_QUEUE_ABONENT_PER_REBILD" ENABLE