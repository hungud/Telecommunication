
  CREATE OR REPLACE TRIGGER "SIM_TRADE"."TIU_BEELINE_STATUS_CODE" 
  BEFORE INSERT OR UPDATE ON BEELINE_STATUS_CODE FOR EACH ROW 
--#Version=1
BEGIN 
  IF INSERTING THEN 
    IF NVL(:NEW.STATUS_ID, 0) = 0 then 
      :NEW.STATUS_ID := NEW_STATUS_ID; 
    END IF; 
  END IF;
END; 

ALTER TRIGGER "SIM_TRADE"."TIU_BEELINE_STATUS_CODE" ENABLE