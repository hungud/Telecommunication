
  CREATE OR REPLACE TRIGGER "SIM_TRADE"."TI_SIM_PHONE_BILLS" 
  BEFORE INSERT ON SIM_PHONE_BILLS FOR EACH ROW
--#Version=1
BEGIN
  IF NVL(:NEW.BILL_ID, 0) = 0 then
    :NEW.BILL_ID := NEW_BILL_ID;
  END IF;    
END; 

ALTER TRIGGER "SIM_TRADE"."TI_SIM_PHONE_BILLS" ENABLE