create GLOBAL TEMPORARY TABLE LOAD_BILL_TEMP
(
 
  PHONE_ID INTEGER

);

/*
ON COMMIT PRESERVE ROWS
  GET_PHONE_ID(Extract_Word(str, C_SELF_NUMBER_IDX));
        
        
        := Extract_Word(str, C_SERVICE_DATE_IDX);
        := Extract_Word(str, C_SERVICE_TIME_IDX);
        := Extract_Word(str, C_OTHER_NUMBER_IDX);
        := Extract_Word(str, C_SERVICE_DIRECTION_IDX);
        := Extract_Word(str, C_SERVICE_TYPE_IDX);
        := Extract_Word(str, C_DURATION_SECONDS_IDX);
        := Extract_Word(str, C_COST_IDX);
        := Extract_Word(str, C_IS_GROUP_IDX);

        := Extract_Word(str, C_IS_ROAMING_IDX);
        := Extract_Word(str, C_SELF_ZONE_IDX);
        := Extract_Word(str, C_OTHER_ZONE_IDX);
        := Extract_Word(str, C_ADD_INFO_IDX);
        := Extract_Word(str, C_ACCOUNT_NO_IDX);
--        := Extract_Word(str, C_LINE_POS_IDX);
--        := Extract_Word(str, C_LINE_LENGTH_IDX);
        := Extract_Word(str, C_ORIGIN_COST_IDX);
        := Extract_Word(str, C_COST_WITHOUT_VAT_IDX);
        := Extract_Word(str, C_BILL_NUMBER_IDX);
        := Extract_Word(str, C_BILL_DATE_IDX);
        := Extract_Word(str, C_PHONE_LIMIT_IDX);
        := Extract_Word(str, C_PHONE_CHARGE_LIMIT_IDX);
        := Extract_Word(str, C_OPTION_IDX);
        := Extract_Word(str, C_CASCADE_NUMBER_IDX);
*/        