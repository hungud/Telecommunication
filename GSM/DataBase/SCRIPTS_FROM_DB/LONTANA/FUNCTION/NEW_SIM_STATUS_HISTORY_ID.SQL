
  CREATE OR REPLACE FUNCTION "LONTANA"."NEW_SIM_STATUS_HISTORY_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_SIM_STATUS_ID.NEXTVAL
    INTO vRES
    FROM DUAL;
  RETURN vRES;
END; 