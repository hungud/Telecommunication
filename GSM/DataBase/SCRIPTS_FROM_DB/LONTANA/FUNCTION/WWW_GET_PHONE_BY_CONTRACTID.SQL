
  CREATE OR REPLACE FUNCTION "LONTANA"."WWW_GET_PHONE_BY_CONTRACTID" (
  pCONTRACT_ID IN INTEGER
  ) RETURN VARCHAR2 IS
--#Version=1
vPhone varchar2(10);
BEGIN
  SELECT PHONE_NUMBER_FEDERAL
  into vPhone
    FROM CONTRACTS
    WHERE CONTRACT_ID=pCONTRACT_ID ;
  RETURN vPhone; 
END;