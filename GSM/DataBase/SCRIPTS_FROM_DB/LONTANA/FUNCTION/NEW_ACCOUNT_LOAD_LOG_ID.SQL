
  CREATE OR REPLACE FUNCTION "LONTANA"."NEW_ACCOUNT_LOAD_LOG_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  --raise_application_error(-20000, '!');
  SELECT S_NEW_ACCOUNT_LOAD_LOG_ID.NEXTVAL
  INTO vRES
  FROM DUAL;
  RETURN vRES;
END; 