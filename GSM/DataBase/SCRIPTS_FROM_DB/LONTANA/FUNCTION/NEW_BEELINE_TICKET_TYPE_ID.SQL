
  CREATE OR REPLACE FUNCTION "LONTANA"."NEW_BEELINE_TICKET_TYPE_ID" RETURN NUMBER IS
--#Version=1
  vRES NUMBER;
BEGIN
  SELECT S_NEW_BEELINE_TICKET_TYPE_ID.NEXTVAL
  INTO vRES
  FROM DUAL;
  RETURN vRES;
END;