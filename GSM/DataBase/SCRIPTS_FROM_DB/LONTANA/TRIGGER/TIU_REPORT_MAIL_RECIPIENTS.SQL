
  CREATE OR REPLACE TRIGGER "LONTANA"."TIU_REPORT_MAIL_RECIPIENTS" 
  BEFORE INSERT OR UPDATE ON REPORT_MAIL_RECIPIENTS FOR EACH ROW
--#Version=1
BEGIN
  IF INSERTING THEN
    IF NVL(:NEW.RECORD_ID, 0) = 0 then
      :NEW.RECORD_ID := NEW_RECORD_ID;
    END IF;
  END IF;
END; 
ALTER TRIGGER "LONTANA"."TIU_REPORT_MAIL_RECIPIENTS" ENABLE