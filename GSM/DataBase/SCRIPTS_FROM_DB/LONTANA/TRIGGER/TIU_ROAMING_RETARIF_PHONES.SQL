
  CREATE OR REPLACE TRIGGER "LONTANA"."TIU_ROAMING_RETARIF_PHONES" 
  BEFORE INSERT OR UPDATE ON ROAMING_RETARIF_PHONES FOR EACH ROW 
--#Version=1
BEGIN 
  IF INSERTING THEN 
    IF NVL(:NEW.ID, 0) = 0 then 
      :NEW.ID := S_NEW_ROAMING_RETARIF_PHONE_ID.NEXTVAL; 
    END IF; 
    :NEW.USER_CREATED := USER; 
    :NEW.DATE_CREATED := SYSDATE;
  END IF; 
  :NEW.USER_LAST_UPDATED := USER; 
  :NEW.DATE_LAST_UPDATED := SYSDATE; 
END;
--#end if
ALTER TRIGGER "LONTANA"."TIU_ROAMING_RETARIF_PHONES" ENABLE